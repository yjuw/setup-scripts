on: push
  
jobs:
  fedora-test:
    runs-on: ubuntu-latest
    container: fedora:39
    steps:
      - run: |
          sudo chmod +x setup.sh
          ./setup.sh
  ubuntu-test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          sudo chmod +x setup.sh
          ./setup.sh



